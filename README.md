# IG01
IG01 (Instrument Grid) — A 16×16 grid sequencer in one HTML file. Each cell is a note: its own scale, wave, volume, decay. 35 pitch mappings, 8 pattern pages, Web Audio lookahead engine, ambient randomization. Monochrome CRT interface. No dependencies. No build step.
